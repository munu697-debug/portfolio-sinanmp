<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AJZAL.M | Admin Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        /* --- DESIGN SYSTEM & RESET --- */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Plus Jakarta Sans', sans-serif;
        }

        body {
            /* Smooth background gradient matching the portfolio's deep slate/purple vibe */
            background: radial-gradient(circle at 50% 50%, #13101C 0%, #0B0813 100%);
            color: #FFFFFF;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            overflow: hidden;
            position: relative;
        }

        /* Ambient Glow Effects in Background */
        body::before {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            background: #00E676;
            opacity: 0.05;
            filter: blur(120px);
            top: 20%;
            left: 25%;
            border-radius: 50%;
        }

        /* --- LOGIN CARD --- */
        .login-container {
            width: 100%;
            max-width: 440px;
            background: rgba(19, 16, 28, 0.75);
            border: 1px solid rgba(38, 34, 53, 0.8);
            backdrop-filter: blur(16px);
            border-radius: 24px;
            padding: 40px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
            z-index: 1;
        }

        /* Brand Typography */
        .brand {
            font-size: 22px;
            font-weight: 700;
            letter-spacing: 1px;
            text-align: center;
            margin-bottom: 24px;
        }

        .brand span {
            color: #00E676; /* Accent Neon Green */
        }

        .login-header {
            text-align: center;
            margin-bottom: 32px;
        }

        .login-header h1 {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 8px;
        }

        .login-header p {
            color: #94A3B8;
            font-size: 13px;
            line-height: 1.5;
        }

        /* --- FORM ELEMENTS --- */
        .form-group {
            margin-bottom: 20px;
            position: relative;
        }

        .form-group label {
            display: block;
            font-size: 13px;
            font-weight: 500;
            color: #94A3B8;
            margin-bottom: 8px;
        }

        .input-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }

        .input-wrapper i {
            position: absolute;
            left: 16px;
            color: #64748B;
            font-size: 16px;
            transition: color 0.3s ease;
        }

        .input-wrapper input {
            width: 100%;
            padding: 14px 16px 14px 44px;
            background: #1A1625;
            border: 1px solid #2C2838;
            border-radius: 12px;
            color: #FFFFFF;
            font-size: 14px;
            outline: none;
            transition: all 0.3s ease;
        }

        /* Input Interaction States */
        .input-wrapper input:focus {
            border-color: #00E676;
            box-shadow: 0 0 12px rgba(0, 230, 118, 0.15);
        }

        .input-wrapper input:focus + i {
            color: #00E676;
        }

        /* --- BUTTONS --- */
        .btn-group {
            display: flex;
            gap: 12px;
            margin-top: 28px;
        }

        .btn {
            flex: 1;
            padding: 14px;
            border-radius: 12px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 8px;
            transition: all 0.2s ease;
            border: none;
        }

        .btn-login {
            background: #00E676;
            color: #0B0813;
        }

        .btn-login:hover {
            opacity: 0.95;
            box-shadow: 0 0 20px rgba(0, 230, 118, 0.35);
        }

        .btn-clear {
            background: transparent;
            color: #94A3B8;
            border: 1px solid #2C2838;
        }

        .btn-clear:hover {
            background: rgba(255, 255, 255, 0.03);
            color: #FFFFFF;
            border-color: #4A455A;
        }

        /* --- SYSTEM INFO BOX --- */
        .info-box {
            margin-top: 24px;
            background: rgba(0, 230, 118, 0.04);
            border: 1px dashed rgba(0, 230, 118, 0.2);
            border-radius: 12px;
            padding: 12px 16px;
            font-size: 12px;
            color: #94A3B8;
            line-height: 1.6;
        }

        .info-box strong {
            color: #00E676;
            font-weight: 500;
        }
    </style>
</head>
<body>

    <div class="login-container">
        <div class="brand">AJZAL<span>.</span>M</div>

        <div class="login-header">
            <h1>Admin Panel</h1>
            <p>Admin edits are stored in Realtime Database.</p>
        </div>

        <form onsubmit="event.preventDefault();">
            <div class="form-group">
                <label>Admin Username</label>
                <div class="input-wrapper">
                    <input type="email" placeholder="Enter email" required value="mpsinanomr@gmail.com">
                    <i class="fa-solid fa-envelope"></i>
                </div>
            </div>

            <div class="form-group">
                <label>Admin Password</label>
                <div class="input-wrapper">
                    <input type="password" placeholder="Enter password" required value="Getintopc@123">
                    <i class="fa-solid fa-lock"></i>
                </div>
            </div>

            <div class="btn-group">
                <button type="button" class="btn btn-clear" onclick="document.querySelector('form').reset();">
                    Clear Data
                </button>
                <button type="submit" class="btn btn-login">
                    Login <i class="fa-solid fa-arrow-right"></i>
                </button>
            </div>
        </form>

        <div class="info-box">
            <i class="fa-solid fa-key" style="color: #00E676; margin-right: 6px;"></i>
            Use username: <strong>mpsinanomr@gmail.com</strong> and password: <strong>Getintopc@123</strong>
        </div>
    </div>

</body>
</html>